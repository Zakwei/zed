[package]
name = "msvc_spectre_libs"
version = "0.1.3"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "No-op stub replacing the msvc_spectre_libs build script (aitagent fork)."

[lib]
path = "src/lib.rs"

[features]
error = []
